services:
  - type: web
    name: parcel-os-api
    runtime: node
    buildCommand: npm ci && npm run prisma:generate && npm run prisma:push && npm run build
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_REFRESH_SECRET
        generateValue: true
